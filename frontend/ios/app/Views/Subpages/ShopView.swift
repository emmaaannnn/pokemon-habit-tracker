import SwiftUI

struct ShopView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Welcome Shop!")
                    .font(.title)
            }
            .navigationTitle("Shop")
        }
    }
}
